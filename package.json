{
    "name": "daily-expenses-detector-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "start": "cd backend && npm start",
        "dev": "cd backend && npm run dev",
        "install:backend": "cd backend && npm install",
        "setup": "npm run install:backend"
    }
}